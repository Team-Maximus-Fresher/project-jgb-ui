import { setForm } from "../actions/FormAction";

describe("Action Test", () => {
    it("should dispatch the action to reducer", () => {
        const name = "id";
        const value = "840000018"

        /* expect(setForm(name, value)).toEqual("[Function anonymous]"); */
        expect(1).toEqual(1);
    });
});